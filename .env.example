PORT=3000
HOST=127.0.0.1
DATA_DIR=./data
# The journal opens directly without a password.
REQUIRE_PASSWORD=false
# Optional: only used when REQUIRE_PASSWORD=true.
JOURNAL_PASSWORD=
# Required when exposing the app on the internet:
# PUBLIC_ORIGIN=https://journal.example.ru
# COOKIE_SECURE=true
