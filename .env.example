PORT=3000
HOST=127.0.0.1
DATA_DIR=./data
# Set a strong password (12 or more characters) before the first start.
JOURNAL_PASSWORD=
# Required when exposing the app on the internet:
# PUBLIC_ORIGIN=https://journal.example.ru
# COOKIE_SECURE=true
