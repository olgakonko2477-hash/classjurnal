{
  "name": "teacher-journal",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=24.0.0"
  },
  "scripts": {
    "start": "node --env-file-if-exists=.env server.js",
    "test": "node --test tests/*.test.js",
    "test:browser": "node tests/browser.mjs",
    "build": "node scripts/build-sites.js",
    "test:cloud": "node --test tests/cloud.mjs"
  },
  "dependencies": {
    "exceljs": "^4.4.0"
  },
  "devDependencies": {
    "@playwright/test": "^1.63.0",
    "esbuild": "^0.28.2",
    "miniflare": "^5.20260921.0-alpha"
  },
  "overrides": {
    "exceljs": {
      "uuid": "11.1.1"
    }
  }
}
